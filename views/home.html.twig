{% extends 'layout.html.twig' %}

{% block page_title %}Cool Framework{% endblock %}

{% block content %}
<h1>
  Welcome to the Cool Framework<br>
  <small>By SUP'Internet DEV 2020</small>
    {{ username }}
  </h1>
  <br>
  {% set foo =  0 %}
  {% for file in files %}
        <a download={{file}} href={{paths[foo]}}> {{file}}</a>
        <a href='?action=home&deletefile={{file}}&path={{path}}'>❌</a>
        <a href='?action=rename&name={{file}}&path={{path}}'> 🖊</a>
        <a href='?action=move&path={{path}}&name={{file}}'> <img  width="22px" src="./assets/img/move.png"></a>
        {% if valid[foo] == "valid" or valid[foo]== "text" %}
        <a href='?action=see&path={{path}}&name={{file}}'><img  width="22px" src="./assets/img/see.png"></a>  
        {% endif %}
        {% if valid[foo] == "text" %}
        <a href='?action=write&path={{path}}&name={{file}}'><img  width="22px" src="./assets/img/write.png"></a>
        {% endif %}
        <br>
        {% set foo = foo + 1 %}
  {% endfor %}
    {% for folder in folders %}
      <a href='?action=home&path={{path}}{{folder}}'>{{folder}}</a>
      <a href='?action=home&path={{path}}&deletefile={{folder}}'> ❌</a>
      <a href='?action=rename&path={{path}}&name={{folder}}'> 🖊</a>
      <a href='?action=move&path={{path}}&name={{folder}}'> <img  width="22px" src="./assets/img/move.png"></a>  <br> <br>
  {% endfor %}
 {%if username is defined%}
    <a href='?action=home&path={{parentpath}}'>Back to the 90's</a>
    {% endif %}
{% endblock %}
